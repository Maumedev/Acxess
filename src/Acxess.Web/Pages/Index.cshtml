@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

@* <div class="text-center">
    <h1 class="display-4">Welcome to Acxess</h1>
</div> *@


<div class="space-y-8 p-10">
    
    <div x-data="{ cuenta: 0 }" class="p-6 border rounded-lg shadow-md max-w-sm">
        <h3 class="text-lg font-bold mb-2">Contador Alpine</h3>
        
        <p class="mb-4">
            Valor actual: <span x-text="cuenta" class="font-mono text-2xl text-blue-600 font-bold"></span>
        </p>

        <div class="flex gap-2">
            <button @@click="cuenta++" class="px-4 py-2 bg-green-500 text-white rounded hover:bg-green-600">
                Sumar +
            </button>
            <button @@click="cuenta = 0" class="px-4 py-2 bg-gray-500 text-white rounded hover:bg-gray-600">
                Reset
            </button>
        </div>
    </div>

    <div x-data="{ abierto: false }" class="p-6 border rounded-lg shadow-md max-w-sm">
        <div class="flex justify-between items-center mb-2">
            <h3 class="text-lg font-bold">Información Secreta</h3>
            
            <button @@click="abierto = !abierto" class="text-blue-500 hover:underline text-sm">
                <span x-text="abierto ? 'Ocultar' : 'Mostrar'"></span>
            </button>
        </div>

        <div x-show="abierto" x-transition class="bg-yellow-50 p-4 rounded text-yellow-800 text-sm">
            👻 ¡Aquí está el contenido oculto! Alpine maneja esto sin llamar al servidor.
        </div>
    </div>

</div>